{% extends "::base.html.twig"%}
{% block body%}
       <!-- Page Content -->
    <div class="container">

        <!-- Page Heading/Breadcrumbs -->
        <div class="row">
            <div class="col-lg-12">
                
                <ol class="breadcrumb">
                    <li><a href="#">Accueil</a>
                    </li>
                    <li class="active">Présentation</li>
                </ol>
            </div>
        </div>
        <!-- /.row -->

        <!-- Project One -->
        <div class="row">
            <div class="col-md-7">
                <a href="portfolio-item.html">
                    <img src="{{ asset(produit.image.AssetPath) }}" width="500px">
                </a>
            </div>
            <div class="col-md-5">
				  
                <h3>{{produit.nom}}</h3>
                <h4>Subheading</h4>
                <p>{{produit.description}}</p>
                <form action="{{path('ecommerce_ecommerce_ajout_panier', { 'id' : produit.id })}}" method='get'>
					

		<select name="qte" class="span1">
                            {% for i in 1..10 %}
			<option value="{{i}}">{{i}}</option>
                               {% endfor%}
			</select>

			<div>
		  <button class="btn btn-primary">Ajouter au panier</button>
		</div>
		</form>
            </div>
        </div>
        <!-- /.row -->

        <hr>
        
        
                    <div class="col-md-5">

        
<div id="cart-dropdown" hidden>
           
                    </div>
{%endblock%}
{% block javascript %}

{% endblock %}